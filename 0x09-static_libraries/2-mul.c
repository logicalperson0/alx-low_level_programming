/**
 * mul - returns muliply of 2 integers
 *
 * @a: The character in ASCII code
 * @b: The character in ASCII code
 *
 * Return: 1 for lowercase character. 0 for the rest.
 */
int mul(int a, int b)
{
	int i;

	i = a * b;

	return (i);
}
