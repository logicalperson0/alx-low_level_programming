#include "main.h"
/**
 * add - prints addition of 2 numbers
 *
 * @i: The character in ASCII code
 * @j: The caharacter in ASCII code
 *
 * Return: 1 for (Success) 0 for the rest of the cases.
 */
int add(int i, int j)
{
	int k;

	k = i + j;
	return (k);
}
